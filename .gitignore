.DS_Store
Icon?
.ipynb_checkpoints/
__pycache__/
*.pyc

# Fichiers sources telecharges par les build_data.py, jamais commites :
# le classeur retail (45 Mo) et le CSV telco brut. Les rejouer :
#     pipenv run python bloc2_donnees/data/build_data.py
_source_*

# --- Ce qui reste LOCAL -------------------------------------------------
# Le depot public est purement etudiant. Tout ce qui contient des notes de
# passation, des indices a ne pas donner trop tot ou des solutions hors
# correction reste sur la machine de l'enseignant.

# Notebooks intervenant : minutage, notes de passation, pieges attendus.
# Le motif couvre TOUS les blocs, y compris ceux qui n'existent pas encore :
# un chemin fige sur bloc2 publierait les notes du bloc 3 le jour de sa
# creation, sans que personne ne s'en apercoive.
bloc*/intervenant/

# La chaine de generation : outils/contenu/<bloc>/seanceN.py contient MOT POUR MOT
# les notes intervenant et toutes les solutions. La publier reviendrait a
# publier les notebooks intervenant.
outils/

# Environnement de travail local de l'enseignant. Un etudiant n'installe rien :
# tout se passe dans Colab. Publier un Pipfile laisserait croire le contraire.
Pipfile
Pipfile.lock
.venv/

# Dossier a deposer dans Google Drive : copies derivees des notebooks, sans
# badge Colab. Regenerable par `python outils/construire_drive.py`, et le
# publier ici ne ferait que dupliquer le contenu du depot.
distribution_drive/

# Brouillons de travail deposes a la main (notebooks annotes, versions
# ameliorees a reporter dans outils/contenu/). Ils portent des solutions et
# des notes de passation : le depot public n'en veut pas.
improvements/

# --- PUBLICATION PROGRESSIVE (genere par outils/publier.py) ---------------
# Ne pas editer a la main : reecrit a chaque deplacement du curseur.
# Les fichiers ci-dessous existent sur le disque mais ne sont pas encore
# ouverts aux etudiants. Les ignorer empeche un `git add -A` de les publier
# par accident.
bloc2_donnees/corrections/seance1_correction.ipynb
bloc2_donnees/corrections/seance2_correction.ipynb
bloc2_donnees/cours/seance1_cours.ipynb
bloc2_donnees/cours/seance2_cours.ipynb
bloc2_donnees/data/build_data.py
bloc2_donnees/data/clients.csv
bloc2_donnees/data/produits.csv
bloc2_donnees/data/ventes.csv
bloc2_donnees/data/ventes_sale.csv
bloc2_donnees/exercices/seance1_exercices.ipynb
bloc2_donnees/exercices/seance2_exercices.ipynb
bloc3_ml/corrections/seance1_correction.ipynb
bloc3_ml/corrections/seance2_correction.ipynb
bloc3_ml/cours/seance1_cours.ipynb
bloc3_ml/cours/seance2_cours.ipynb
bloc3_ml/cours/seance3_cours.ipynb
bloc3_ml/cours/seance4_cours.ipynb
bloc3_ml/data/build_data.py
bloc3_ml/data/churn.csv
bloc3_ml/data/clients_rfm.csv
bloc3_ml/data/commandes.csv
bloc3_ml/data/produits_profil.csv
bloc3_ml/data/tickets.csv
bloc3_ml/data/tickets_nouveaux.csv
bloc3_ml/exercices/seance1_exercices.ipynb
bloc3_ml/exercices/seance2_exercices.ipynb
# --- fin PUBLICATION PROGRESSIVE ------------------------------------------
